# 贡献指南

## 基本流程

1. Fork 并创建功能分支。
2. 一次提交聚焦单一问题。
3. 行为变更必须同步更新文档。
4. 提交 PR 前完成本地检查。

## 本地检查

```sh
./scripts/check.sh
```

## 提交信息建议

- `core:` 核心领域/运行时变更
- `ffi:` C ABI 或跨语言边界变更
- `docs:` 文档变更
- `build:` 构建或脚本变更

## 兼容性规则

- C ABI 破坏性变更必须提升版本并提供迁移说明。
- 新增公开接口必须带输入校验与错误码定义。
